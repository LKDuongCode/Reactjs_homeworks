enum weekDays {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}

let allDays: weekDays[] = [
    weekDays.MONDAY,
    weekDays.TUESDAY,
    weekDays.WEDNESDAY,
    weekDays.THURSDAY,
    weekDays.FRIDAY,
    weekDays.SATURDAY,
    weekDays.SUNDAY
];

for (let day of allDays) {
    console.log(day);
}
