async function PrimeWithPromise(numb:number) {
    let promise = new Promise<void>((resolve, reject) => {
        if(numb >= 2){
            for(let i = 2;i<Math.sqrt(numb);i++){
                
            }
        }
    })
}